import { readAllLines } from './io.util';

const part1 = (input: string[]): number => {
  return -1;
};

const part2 = (input: string[]): number => {
  return -1;
};

const input = readAllLines('./res/test_input.txt');
console.log('part1:', part1(input));
console.log('part2:', part2(input));
